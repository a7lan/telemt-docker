# telemt Docker image

This repository builds and publishes a Docker image for the upstream project  
[telemt/telemt](https://github.com/telemt/telemt).

The application source code is **not maintained here**.  
This repository is used only to package the upstream project into a Docker image.

📦 Docker image:  
https://hub.docker.com/r/a7lanov/telemt

Images are built automatically from upstream releases and published with version tags and `latest`.

⚠️ This is not an official image of the telemt project.